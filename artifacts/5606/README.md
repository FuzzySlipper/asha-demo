# Task 5606 live telemetry proof

`asha-demo-live-telemetry-overlay.png` is captured by the native Chromium live
test after accepted primary fire. The test also reads the same generated
machine snapshot, verifies unavailable counters are explicit rather than
fabricated, and proves the projection-local visibility toggle leaves the
snapshot unchanged.
