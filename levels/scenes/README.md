# Scene Documents

`generated-tunnel-room.scene.json` is the durable `SceneDocument` loaded by the
served demo. It places the player and enemy `EntityDefinition`s by spawn marker
and refs the level preset, spawn catalog, and material catalog.
