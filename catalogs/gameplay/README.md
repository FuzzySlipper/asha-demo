# Gameplay Catalog

`default-fps.catalog.json` records the demo's selected upstream FPS gameplay
preset, hashes, and durable refs to the level, weapon, enemy, and spawn catalog
files used by the served demo.
